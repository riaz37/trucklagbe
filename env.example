# Database Configuration (Prisma format)
DATABASE_URL="mysql://trucklagbe_user:trucklagbe_password@localhost:3306/trucklagbe?connection_limit=50&pool_timeout=20&connect_timeout=60&acquire_timeout=60000&timeout=60000&idle_timeout=60000&reject_unauthorized=false&ssl=false"

# Application Configuration
NODE_ENV=development
PORT=3000

# Performance Settings
NODE_OPTIONS="--max-old-space-size=2048"
UV_THREADPOOL_SIZE=64

# Database Performance
DB_CONNECTION_LIMIT=50
DB_ACQUIRE_TIMEOUT=60000
DB_TIMEOUT=60000
DB_POOL_TIMEOUT=20000
DB_CONNECT_TIMEOUT=60000
DB_IDLE_TIMEOUT=60000
DB_MAX_POOL_SIZE=50
DB_MIN_POOL_SIZE=5

 
# API Configuration
